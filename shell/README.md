# Shell

Zsh/Bash config: aliases, PATH setup, prompt, and any agent-related
environment variables (e.g. tokens exported for CLI tools).

## TODO

- [ ] Add `.zshrc` / `.bashrc` (as `.example` with secrets stripped)
- [ ] Document any custom functions used for agentic workflows
